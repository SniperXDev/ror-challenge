<div class="bg-blue-50 p-6 rounded-lg shadow-md">
  <p class="text-lg mb-4">Hello <%= @resource.email %>!</p>
  <p class="text-base mb-4">Someone has requested a link to change your password. You can do this through the link below:</p>
  
  <div class="mb-4">
    <%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token), class: 'text-blue-500 hover:underline font-semibold' %>
  </div>

  <p class="text-base mb-4">If you didn't request this, please ignore this email.</p>
  <p class="text-base">Your password won't change until you access the link above and create a new one.</p>
</div>
